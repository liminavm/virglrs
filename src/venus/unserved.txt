# The venus commands this build does not serve, and why each one is here.
#
# A guest may legally send any of these. It is offered every extension the host driver has and the
# pinned vk.xml can *serialize* (our `vkEnumerateDeviceExtensionProperties` answer; the capset's
# mask only says which structs its encoder may use), and every core command of the version it is
# told -- and a third of the lines below are core, down to 1.0, which no extension list can take
# back. So the ledger is closed by serving, never by hiding. Each of these poisons its context.
# That is the right answer -- a zeroed reply is shaped exactly like a successful one, so there is
# no safe way to continue -- but it is only the right answer while the set is a decision rather
# than an accident.
#
# This file is that decision, and a bare list of names could not be: it says nothing about whether
# a line is work we owe or a command nothing here can reach. So each line carries a
# `status:group`, and `every_command_the_protocol_defines_is_served_or_on_the_ledger` holds the
# whole file to it -- serving a command means deleting its line, and a protocol bump that adds one
# means adding a line with a status.
#
#   wanted            The reference serves it, this host can run it, and a guest here can emit it.
#                     This is the work, and `group` is the unit to do it in: one probe program per
#                     group, RED against this build and GREEN against the C, then the handlers.
#   out-of-reach      No supported host's guest venus device advertises the extension, so nothing
#                     that runs here can send it. Serving it would mean a `try_` accessor that
#                     refuses by name -- there is no test that could go green. The bar is every
#                     host, not the one at hand: a command out of reach on KosmicKrisp and
#                     reachable on anv is `wanted`, and four groups below moved that way once the
#                     Linux guest was asked.
#   not-in-reference  The C refuses it too. Mostly commands the venus driver answers guest-side
#                     and never puts on the wire. Not a gap against the reference; a decision.
#
# The counts are measured, not estimated: every status was cut against the extension lists of real
# guest venus devices -- one per supported host -- and not against vk.xml or the host driver. The
# Linux guest advertises 169 device extensions on anv, and every `wanted` group's extension is
# among them, so nothing here is wanted on one host and out of reach on the other.
#
# What a line here costs, when a guest reaches it: nothing guest-side names the command. Poisoning
# is the right answer and it is still not a diagnosable one -- the guest's next call fails, its
# driver reports that failure in its own vocabulary, and the shape of that is a resource error.
# `vkQueueSubmit2` was measured: zink reported VK_ERROR_OUT_OF_HOST_MEMORY from
# vkEndCommandBuffer, then "couldn't allocate memory", then GL_OUT_OF_MEMORY in glReadPixels, and
# then deadlocked with every thread in futex_wait on a fence the dead context would never signal.
# The test above it saw only its own timeout. The `[virglrs] refused:` line in the worker log is
# the ONLY place the command is named -- so read that log first, and never diagnose a guest-side
# OOM or hang without it.
#
# A `wanted` line is not a promise that anything reaches it today, and not a promise that nothing
# does. A seated synoik session plus all 33 glmark2 scenes plus two indexed-draw probes refused
# nothing, because zink emits `vkCmdDrawMultiIndexedEXT` -- which this build serves -- wherever
# a plain `vkCmdDrawIndexed` would appear. A stock Fedora 44 desktop, measured
# 2026-09-08, refuses `vkCreateBufferView` repeatedly while it is merely running.
#
# What a Linux guest reaches, measured on anv with the poisoning taken out so a boot could count
# more than one: vkmark's whole scene set, twice over with its non-default options, reaches
# nothing on this list at all. Nor does zink: the one command it sent, from glmark2 on wayland
# and from kmscube on KMS, was `vkCmdSetColorWriteEnableEXT`, which is served. A GNOME desktop
# with a venus client on it -- vkcube composited by gnome-shell -- reaches nothing on this list
# either.
#
# So a workload census cannot enumerate this file: the first unserved command kills the context,
# so a boot discovers exactly one and says nothing about the rest. Reach these deliberately, with
# a program that names the command -- `harness/probes/` is one per group.
#
# And read the worker log, not the program. A guest reads a zeroed reply as success, and venus
# answers some commands guest-side without asking the host at all, so a probe can report `ok` for
# a command that was refused: measured on the events group, where vkCreateEvent was refused and
# the next four checks still passed. `[virglrs] refused:` is the oracle. The program is not.
vkCmdBeginConditionalRenderingEXT                  wanted:conditional-rendering
vkCmdBeginQueryIndexedEXT                          wanted:transform-feedback
vkCmdBeginTransformFeedbackEXT                     wanted:transform-feedback
vkCmdBindResourceHeapEXT                           out-of-reach:descriptor-heap
vkCmdBindSamplerHeapEXT                            out-of-reach:descriptor-heap
vkCmdBindTransformFeedbackBuffersEXT               wanted:transform-feedback
vkCmdBuildAccelerationStructuresIndirectKHR        out-of-reach:acceleration-structure
vkCmdBuildAccelerationStructuresKHR                out-of-reach:acceleration-structure
vkCmdCopyAccelerationStructureKHR                  out-of-reach:acceleration-structure
vkCmdCopyAccelerationStructureToMemoryKHR          out-of-reach:acceleration-structure
vkCmdCopyMemoryToAccelerationStructureKHR          out-of-reach:acceleration-structure
vkCmdDrawIndirectByteCountEXT                      wanted:transform-feedback
vkCmdDrawMeshTasksEXT                              out-of-reach:mesh-shader
vkCmdDrawMeshTasksIndirectCountEXT                 out-of-reach:mesh-shader
vkCmdDrawMeshTasksIndirectEXT                      out-of-reach:mesh-shader
vkCmdEndConditionalRenderingEXT                    wanted:conditional-rendering
vkCmdEndQueryIndexedEXT                            wanted:transform-feedback
vkCmdEndRendering2KHR                              out-of-reach:maintenance10
vkCmdEndTransformFeedbackEXT                       wanted:transform-feedback
vkCmdExecuteCommands                               wanted:secondary-command-buffers
vkCmdPushDataEXT                                   out-of-reach:descriptor-heap
vkCmdSetAlphaToCoverageEnableEXT                   wanted:extended-dynamic-state3
vkCmdSetAlphaToOneEnableEXT                        wanted:extended-dynamic-state3
vkCmdSetColorBlendAdvancedEXT                      wanted:extended-dynamic-state3
vkCmdSetColorBlendEnableEXT                        wanted:extended-dynamic-state3
vkCmdSetColorBlendEquationEXT                      wanted:extended-dynamic-state3
vkCmdSetColorWriteMaskEXT                          wanted:extended-dynamic-state3
vkCmdSetConservativeRasterizationModeEXT           wanted:extended-dynamic-state3
vkCmdSetDepthClampEnableEXT                        wanted:extended-dynamic-state3
vkCmdSetDepthClampRangeEXT                         out-of-reach:shader-object
vkCmdSetDepthClipEnableEXT                         wanted:extended-dynamic-state3
vkCmdSetDepthClipNegativeOneToOneEXT               wanted:extended-dynamic-state3
vkCmdSetExtraPrimitiveOverestimationSizeEXT        wanted:extended-dynamic-state3
vkCmdSetFragmentShadingRateKHR                     wanted:fragment-shading-rate
vkCmdSetLineRasterizationModeEXT                   wanted:extended-dynamic-state3
vkCmdSetLineStippleEnableEXT                       wanted:extended-dynamic-state3
vkCmdSetLogicOpEnableEXT                           wanted:extended-dynamic-state3
vkCmdSetPolygonModeEXT                             wanted:extended-dynamic-state3
vkCmdSetProvokingVertexModeEXT                     wanted:extended-dynamic-state3
vkCmdSetRasterizationSamplesEXT                    wanted:extended-dynamic-state3
vkCmdSetRasterizationStreamEXT                     wanted:extended-dynamic-state3
vkCmdSetRayTracingPipelineStackSizeKHR             out-of-reach:ray-tracing-pipeline
vkCmdSetSampleLocationsEnableEXT                   wanted:extended-dynamic-state3
vkCmdSetSampleMaskEXT                              wanted:extended-dynamic-state3
vkCmdSetTessellationDomainOriginEXT                wanted:extended-dynamic-state3
vkCmdSetVertexInputEXT                             wanted:vertex-input-dynamic-state
vkCmdTraceRaysIndirect2KHR                         out-of-reach:ray-tracing-maintenance1
vkCmdTraceRaysIndirectKHR                          out-of-reach:ray-tracing-pipeline
vkCmdTraceRaysKHR                                  out-of-reach:ray-tracing-pipeline
vkCmdWriteAccelerationStructuresPropertiesKHR      out-of-reach:acceleration-structure
vkCopyAccelerationStructureKHR                     not-in-reference:acceleration-structure
vkCreateAccelerationStructureKHR                   out-of-reach:acceleration-structure
vkCreateDeferredOperationKHR                       not-in-reference:deferred-operation
vkCreatePrivateDataSlot                            not-in-reference:private-data
vkCreateRayTracingPipelinesKHR                     out-of-reach:ray-tracing-pipeline
vkDeferredOperationJoinKHR                         not-in-reference:deferred-operation
vkDestroyAccelerationStructureKHR                  out-of-reach:acceleration-structure
vkDestroyDeferredOperationKHR                      not-in-reference:deferred-operation
vkDestroyPrivateDataSlot                           not-in-reference:private-data
vkEnumerateDeviceLayerProperties                   not-in-reference:layer-enumeration
vkEnumerateInstanceLayerProperties                 not-in-reference:layer-enumeration
vkGetAccelerationStructureBuildSizesKHR            out-of-reach:acceleration-structure
vkGetAccelerationStructureDeviceAddressKHR         out-of-reach:acceleration-structure
vkGetDeferredOperationMaxConcurrencyKHR            not-in-reference:deferred-operation
vkGetDeferredOperationResultKHR                    not-in-reference:deferred-operation
vkGetDeviceAccelerationStructureCompatibilityKHR   out-of-reach:acceleration-structure
vkGetDeviceQueue                                   not-in-reference:device-queue
vkGetImageOpaqueCaptureDataEXT                     out-of-reach:descriptor-heap
vkGetPhysicalDeviceCooperativeMatrixPropertiesKHR  out-of-reach:cooperative-matrix
vkGetPhysicalDeviceDescriptorSizeEXT               out-of-reach:descriptor-heap
vkGetPhysicalDeviceFragmentShadingRatesKHR         wanted:fragment-shading-rate
vkGetPrivateData                                   not-in-reference:private-data
vkGetRayTracingCaptureReplayShaderGroupHandlesKHR  out-of-reach:ray-tracing-pipeline
vkGetRayTracingShaderGroupHandlesKHR               out-of-reach:ray-tracing-pipeline
vkGetRayTracingShaderGroupStackSizeKHR             out-of-reach:ray-tracing-pipeline
vkQueueBindSparse                                  wanted:sparse-binding
vkRegisterCustomBorderColorEXT                     out-of-reach:descriptor-heap
vkSetPrivateData                                   not-in-reference:private-data
vkTrimCommandPool                                  wanted:secondary-command-buffers
vkUnmapMemory                                      not-in-reference:memory-unmap
vkUnmapMemory2                                     not-in-reference:memory-unmap
vkUnregisterCustomBorderColorEXT                   out-of-reach:descriptor-heap
vkWriteAccelerationStructuresPropertiesKHR         not-in-reference:acceleration-structure
vkWriteResourceDescriptorMESA                      out-of-reach:descriptor-heap
vkWriteSamplerDescriptorMESA                       out-of-reach:descriptor-heap
