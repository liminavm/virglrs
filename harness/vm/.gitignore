# Everything in this directory is a large local artifact — bundles, disk images,
# build prefixes, captures. Only the scripts, the workload pages and the README are tracked.
*
!.gitignore
!README.md
!*.sh
!*.py
!*.html
